# 帝成观止 · 对抗性审查报告（2026-08-24 第四轮）

> 审计对象：`E:\CardGame\app`（Vite + React 19 + TS 叙事卡牌引擎）+ `src/data/*`（13 部剧本全量）+ `src/assets/**`（美术落位）
> 审计立场：**对抗性第四轮复审** —— 以《对抗性审查报告-2026-08-24第三轮》的九条建议为基准，逐条复核并寻找新暴露面。
> 审计方式：通读 `src/engine/*`（duel 334 / runtime 115 / types 243 / save 271 / minigames 152 行）、`src/App.tsx`（1816 行）、`src/data/*` 全量、`scripts/verify.mts`、`src/data/empireShop.ts`、`docs/SCHEMA.md` + **运行时实测**：
> 1. `verify.mts` → **0 失败 / 0 警告**（含 8 处设计性死局按预期标注）
> 2. `tsc -b` → 退出码 **0**
> 3. 引擎行为实证脚本 ×2（情绪制反馈链路 / 物品卡消耗语义 / 行令必胜性枚举 4³=64 序列）
> 4. 数据层定向扫描脚本 ×1（classic 卡层混入 / unlockCard 指向资源卡 / removeCard×钥匙卡 / 货架 0 两 / 卡包孤品 / 残局行宽 / 翻牌 next2 冲突）
> 结论一句话：**第三轮九条建议全部落地（历轮首次"全清"），引擎与校验历史最好；本轮新暴露面聚焦一处 P1 UI 合同断链（情绪制反馈文本丢失，波及 13/23 个对局）与三处 P2 防线的逻辑缺口，无不可玩级缺陷。**

---

## 〇、第三轮回执复核（先验旧账）

| 第三轮建议 | 本轮复核 | 判定 |
|---|---|---|
| P2 · 秦淮楼庸结局被一次性行令运气门控 | 入口选项文案已改「**败可再战**」，`done_jiuling` 改为 **胜后**置旗（`qinhuai.ts:165`、`jl_win` effects）——败走 `jl_lose` 后回 hub 可重入 | ✅ 已修（运气门→可重试时间门，残余 3.1% 单局必输见 2.2） |
| P2 · 市集/翻牌/卡包引用无校验 | `verify.mts` 新增第 7 组断言（货架∪卡包池∪翻牌选项 → 卡牌表；翻牌选项数 warn） | ✅ 属实 |
| P2 · `jiulingPlay` 消耗错牌（潜伏） | 已改为「按索引删除第一张所出花色」（`minigames.ts:141` 注释明示旧实现缺陷） | ✅ 属实 |
| P3 · 图鉴分母含资源卡，进度永不满 | `CardGallery` 分母改 `collectible.length`，资源卡组从图鉴剔除（`App.tsx:1466-1476`） | ✅ 属实 |
| P3 · 气力加成文案超卖 / 回气无上限 | `empireShop.ts` 文案改「本局**压制制**对局气力上限 +2」；回气钳制 `Math.min(10, qi+3)` / `Math.min(hpMax, hp+3)`（`duel.ts:252-255`） | ✅ 属实 |
| P3 · 0 两孤品上架（i_gongxiang） | `buy()` 对 `price === 0` 显式 toast「此物非卖」，货架按钮显示「非卖」（`App.tsx:986,1047`） | ✅ 属实 |
| P3 · 道具回合不触发自动存档 | 存档依赖补 `duel?.usedCards.length`（`App.tsx:430`） | ✅ 属实 |
| P3 · 卡包重复开出静默吞银 | 开包结果对重复卡加「已有 · 不入袋」角标（`App.tsx:1016-1018,1053`） | ✅ 属实 |
| P2 遗留 · DuelView 状态直变 | `clickCard`/`doEndTurn` 全面改 `structuredClone(duel)` 深拷贝再交引擎（`App.tsx:1190,1227`）——**历史最大工程债清偿** | ✅ 属实 |

**判定汇总：9 条 9 条全部属实。未发现任何"声称已修而未修"的虚报。这是历轮审计第一次全清。**

---

## 一、总体结论

| 维度 | 评级 | 一句话 |
|---|---|---|
| 对局引擎（双规则） | ✅ 健康 | 状态直变清偿；baseAp/DEFAULT_GOAL/起手加抽/回气钳制全部落位；真实 UI 合同穷举全绿 |
| 叙事运行时 / 存档 | ✅ 良好 | 结算竞态已修；存档版本+形状校验+隐私容错齐备；读档对局恢复路径完整 |
| 剧情门控 | 🟡 两处残余 | 行令败可再战但 3.1% 单局必输（2.2）；钥匙卡消耗校验盲区（2.3） |
| **对局 UI 反馈** | **🔴 P1 断链** | **情绪制 13/23 个对局的出牌反馈文案全部不可见（2.1）** |
| 经济系统 | ✅ 无软锁面 | 钥匙卡禁卖、0 两非卖、开包重复角标、离市零门槛全生效 |
| 校验体系 | ✅ 对局/门控强 / 🟡 消耗盲区 | 第 7 组引用断言落地；removeCard×钥匙卡仍未建模（2.3） |
| 工程卫生 | 🟡 小账若干 | 抽牌道具不洗弃牌堆（2.4）、行令按钮去重渲染等 |

---

## 二、本轮新发现（按优先级）

### 2.1 【P1】情绪制对局的出牌反馈文本全部丢失（UI 合同断链，实测实证）

**机制链**（逐行核实 + 引擎实证）：
- `playEmotion` 结算后写入 `lastResult`（如「你顺着对方的意，一句“惊堂拍案”接得严丝合缝」「他的防备松动了」）——**引擎层正常**。
- 但 `DuelView.clickCard`（`App.tsx:1194`）在每次出牌后立即调用 `revealEmotion(d)`，而 `revealEmotion` 无条件执行 `st.lastResult = null`（`duel.ts:113`）。
- React 状态更新是批处理的：`setDuel(d)` 提交的永远是「已清空 lastResult 的下一回合状态」，**带反馈文本的中间状态从不渲染**。

实测定性：

| 场景 | 表现 |
|---|---|
| 对局进行中（每轮出牌后） | 共鸣/破防/失言文案 **不显示**，只剩共鸣点数与防备数字的静默变化 |
| 对局胜利 | 「话已说尽，事已办成。」正常显示（`finished` 守卫挡住清理） |
| 对局失败 | 「你词穷气短，这一局，没能拿下。」正常显示（同上） |

影响面：**13 个情绪制对局 / 全库 23 个对局（56%）**，波及 fuma/qiuwei/sichou/xie/qinhuai/jieyu/shumian/changjiang/changhen/xingxing/touming 全部 11 部含情绪局的剧本。对一款以文案为灵魂的权谋叙事游戏，这是核心体验层的实质缺陷——玩家感知不到「话术交锋」的胜负一句。

为何历轮未捕获：`verify.mts` 的 BFS 穷举只断言「可胜性与状态可达」，不渲染 UI；`lastResult` 被清理不影响状态键（`stateKey` 不含 lastResult）。**这是"引擎正确、UI 合同断链"的典型盲区，现有校验体系结构性看不见它。**

修复方向（十行内，建议 ①）：① `revealEmotion` 不再清空 `lastResult`，改由 `playEmotion` 下一轮结算时自然覆盖；② UI 层把 `lastResult` 存独立 state 供本轮展示；③ 对 `verify.mts` 增加断言「情绪局在未 finish 状态下，出牌后 lastResult 非空」（把反馈合同纳入回归）。

### 2.2 【P2】行令「必输序列」仍存在（3.1%），且界面无任何征兆提示

- 64 种令签序列中仍有 **2 种（器器器 / 器器/隐 等，共 2 种）必输**，最差 2 分，离 3 分门槛差 1 分（枚举实证：`4³=64` 全序列最优应对核算，必输 2/64 = 3.1%）。
- 上一轮的「永久锁结局」已解除（败可再战），但**单局内玩家会经历"怎么选都输"的困惑局**——行令界面无任何提示，玩家不知道这一巡牌先天不顺。
- 影响降级为体验层：重复挑战期望约 32 局遇到一次"天谴局"，多数玩家会归因于自己失误而非运气。

修复方向（任选）：① 手牌起点 +1 张备牌（如结尾补一张「隐」）即把最差分抬到 3（需先枚举验证）；② 门槛 `rounds` 改 `rounds-1`，胜负由彩头大小而非成败表达；③ 界面在「翻令签」前提示"此巡牌势不济，可退席重入"。建议 ①（一行数据改动，保持玩法张力）。

### 2.3 【P2】`removeCard` 消耗钥匙卡：机制无防线（当前数据安全，未来地雷）

- `verify` 第 5 组只断言「`cond.card` 引用的卡在剧本内有获得途径」，**不建模"获得后被剧情移除"**。
- 实测命中 3 处 `removeCard` × 钥匙卡：fuma「银票·收入内廷」无条件移除 `i_yinpiao`（之后烧银票线关闭——**设计性分支**，合理）；fuma「烧掉银票」`cond.card=i_yinpiao` 时移除（自我闭合，合理）；qiuwei「埋雷」烧 `i_siyin` 进埋骨结局（**结局消耗**，合理）。
- 当前 3 处均为设计意图，未构成软锁；但若未来作者把「无条件 removeCard」放在「cond.card 分支」之前（顺序写反），钥匙卡在关键选项前被剧情消耗，verify 依然绿灯。
- 建议：verify 5 扩展一条**交叉断言**——收集所有 removeCard 命中的卡 id，与所有 `cond.card/notCard` 引用集求交，命中即 warn（人工确认设计意图），十行内。

### 2.4 【P2】物品「抽牌」卡在牌库空时不洗弃牌堆，抽 0 张

- `drawUp` 的「牌库空 → 洗弃牌堆回填」逻辑没有复用于「抽牌」道具效果（`duel.ts:262-268` 直接从 `library` shift）。
- 全库 5 张抽牌卡受影响：`i_wenji`（qiuwei）/ `i_sangpizhi`（sichou）/ `i_shigao`（xie）/ `i_gongxiang`（qinhuai）等。
- 场景：牌库见底时使用抽牌卡，玩家看到"摸出两张"文案却一张没摸到。语义与 `drawUp` 不一致。
- 修复：抽牌效果前先执行与 `drawUp` 相同的「空库洗回」逻辑，约五行。

### 2.5 【P3】其余工程小账（单条不致命，合并记账）

1. **行令选项按钮用 `[...new Set(hand)]` 去重渲染**（`App.tsx:1378`）：手牌「策器势隐策」显示 4 个按钮而非 5 张——正确性无碍（按花色删除第一张），但玩家感知"手牌 5 张只有 4 个选择"，建议角标显示余量。
2. **`MiniGameView` 渲染期 setState**（`App.tsx:1320-1321` `if (!puzzle) setPuzzle(initPuzzle(...))`）：render-phase update 模式在 React 19 + StrictMode 下会多渲染数次；当前实测稳定，属"能跑但脆弱"模式，重构时建议改 effect 初始化。
3. **经典模式下物品/人物卡的 UI 死面**：数据全量扫描确认 13 部剧本的 classic 对局 deck **零混入**非成术层卡（当前安全），但 `DuelView` 对 classic 手牌中的物品/人物卡仍会渲染可点击按钮（引擎返回 false + toast 兜底）——同 2.3 同类"数据干净时最便宜"的断言，建议 verify 加一行 classic deck 层断言。
4. **`verify.mts` 终端输出在 Windows PowerShell 下中文乱码**（环境问题，非代码缺陷）：`process.stdout` 编码与 cp936 终端不一致，仅影响可读性，不影响退出码。

---

## 三、值得保留的加分项（对抗审计也要记功）

1. **第三轮九条建议全清，且全部"真修"而非"声明修"**：本轮逐条实证，含 structuredClone 对 in-place mutate 的全面替换——历史最大工程债在此结清。
2. **`verify.mts` 已是本项目最可信资产**：七组断言覆盖图完整性/可达性/真实 UI 合同穷举/复盘门控/钥匙卡获得途径/数值上限/小游戏合法性/市集引用，本次四轮审计中唯一持续扩权且从未虚报的组件。
3. **行令门控的修法克制且正确**：不动数值门槛、不删结局，仅把「进即置旗」改为「胜后置旗」+ 入口文案明示"败可再战"，把设计意图（运气参与、但不惩罚探索）完整保留。
4. **钥匙卡禁卖实现依旧是最优解**：运行时扫描 `cond.card/notCard` 动态建集，未来新增钥匙卡自动受保护。
5. **发布物与源码同步**：`dist/assets/index-*.js`（18:43）晚于全部源码 mtime，无陈旧发布风险。

---

## 四、风险优先级清单

| 优先级 | 问题 | 影响 | 修复方向 |
|---|---|---|---|
| P1 | 情绪制出牌反馈文本丢失（`revealEmotion` 清 `lastResult`） | 13/23 个对局（56%）核心反馈文案不可见 | revealEmotion 不清 lastResult / UI 独立保存 / verify 加反馈合同断言 |
| P2 | 行令 3.1% 单局必输且无提示 | 天谴局困惑体验（已非永久锁） | 手牌 +1 备牌 / 门槛降 1 / 界面提示可退席 |
| P2 | removeCard×钥匙卡无校验 | 未来数据写反即软锁且绿灯 | verify 5 增交叉 warn |
| P2 | 抽牌物品卡空库抽 0 张不洗回 | 牌库见底时道具白用 | 复用 drawUp 洗回逻辑 |
| P3 | 行令按钮去重 / 渲染期 setState / classic 层断言 / 终端乱码 | 体验与卫生 | 见 2.5 逐条 |

---

## 五、行动建议（落地顺序）

1. **半天内（全是一行至十行改动）**：① 修 P1 反馈文本断链（2.1，选方案①+③——引擎一行 + verify 一条断言，直接堵住"校验看不见 UI 合同"的结构盲区）；② 抽牌卡洗回（2.4）；③ verify 增 removeCard×钥匙卡交叉 warn（2.3）。
2. **本周（数据调参）**：④ 行令手牌 +1 备牌或界面提示（2.2），改后跑一次 64 序列枚举确认必输序列清零。
3. **回归**：改动后跑 `verify + tsc -b + vite build`，并开一局情绪制对局（如 sichou 任一局）实测出牌后反馈文案可见。

---

*本报告基于通读 `app/src/**`、`docs/SCHEMA.md`、`scripts/*` 及六组实测：`verify.mts`（0 失败/0 警告）、`tsc -b`（退出码 0）、构建产物新鲜度核对、引擎行为实证 ×2、数据层定向扫描 ×1（脚本已删，方法与结论留存于本报告）。与上轮报告不一致处（行令门控处置方式、b_qi 文案）已在第〇章显式标注。*
