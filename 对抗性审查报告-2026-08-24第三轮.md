# 帝成观止 · 对抗性审查报告（2026-08-24 第三轮）

> 审计对象：`E:\CardGame\app`（Vite + React 19 + TS 叙事卡牌引擎）+ `src/assets/**`（美术落位）+ `E:\CardGame\scripts_txt`、`E:\CardGame\art`
> 审计立场：**对抗性第三轮复审** —— 以《对抗性审查报告-2026-08-24复审》（第二轮）的九条修复建议为基准，逐条复核并寻找新暴露面。
> 审计方式：通读 `src/engine/*`（duel 331 / runtime 109 / types 224 / save 272 / minigames 151 行）、`src/App.tsx`（1721 行）、`src/data/*` 全量、`scripts/*`、`docs/SCHEMA.md` + **运行时实测**：
>   1. `verify.mts` → **0 失败 / 0 警告**（含 8 处设计性死局按预期标注）
>   2. `tsc -b` → 退出码 **0**
>   3. `vite build` → 成功（526.44 kB JS，PWA 预缓存 11 项）
>   4. `_audit_coverage.mts` → 卡图/立绘 **0 缺口**，场景图缺 203（覆盖率 ≈ 15%）
>   5. 三个一次性对抗脚本（引用完整性 / cond 传播可达性 / 数值门槛逐条核算 / 行令博弈可胜性，跑完即删）
> 结论一句话：**上轮九条建议八条落地属实（含两条"降级承诺"式处置），核心玩法与校验体系处于历史最好状态；本轮新暴露面集中在「剧情门控与运气的耦合」「引用完整性的校验盲区」「几处实现级小账」，无不可玩级缺陷。**

---

## 〇、上轮回执复核（先验旧账）

| 上轮问题（第二轮报告） | 本轮复核 | 判定 |
|---|---|---|
| P1 · 叙事 8 部卡图=0、场景图覆盖 14% | 卡图 **137 张 + 立绘 26 张，缺口 0**（`_audit_coverage` 实测，封面 13/13、结局图 33/33 亦齐）；**场景图仍 35 张、缺 203（≈15%）** | ✅ 卡图已修复 / ⚠️ 场景图遗留 |
| P1 · 市集可卖钥匙卡（fuma/i_yu） | `ShopView` 扫描全剧本 `cond.card/notCard` 建 `keyIds`，禁卖 + 「案证」角标 + toast（`App.tsx:948-977,1050-1054`）；i_yu 仍在货架但已不可卖 | ✅ 属实 |
| P1 · Excel 管线与 SCHEMA 承诺冲突 | 采用「降级承诺」路径：`SCHEMA.md` 顶部新增 ⚠️ 管线现状标注，逐列列出未支持字段（`SCHEMA.md:3-8`）；`content.mjs` 本身未改 | ✅ 已处置（按建议的备选方案） |
| P2 · verify 元盲区（四组断言） | 四组全部内置：复盘实得线索对账（`verify:192-204`）、钥匙卡获得途径（`verify:205-222`）、`statAtLeast` 理论上限（`verify:223-245`）、小游戏数据合法性（`verify:246-261`） | ✅ 属实 |
| P2 · `endTurn` 吞 `b_ap` 加成 | `baseAp` 记录并回补（`duel.ts:26,97-98,207`） | ✅ 属实 |
| P2 · goal 默认值分裂（引擎 5 / UI 3） | 统一为导出常量 `DEFAULT_GOAL=5`，引擎与 UI 共用（`duel.ts:24`、`App.tsx:1163`） | ✅ 属实 |
| P2 · DuelView 状态直变 + 1.6s 存档竞态 | 竞态已修：对局结束**即刻**推进至结算场景并存档，1.6s 仅留战果定格动画（`App.tsx:326-338`）；**状态直变（in-place mutate + 浅拷贝）未改**（`App.tsx:1122-1144`） | ⚠️ 半修 |
| P3 群 · extraDraw 起手不一致 / 骰宝怪骰 / 死代码 / 数值条零消费 / 根 README | 起手被动加抽已结算（`duel.ts:103`）；控骰改拒绝采样保合法骰面（`minigames.ts:33-38`）；`warehouseAdd/Remove` 已删、仓库字段注释为旧档兼容（`save.ts:156`）；**数值条已被全 13 部消费**（12 处 `statAtLeast` 门槛 + verify 5b 建模）；根 README 已成文 | ✅ 属实 |

**判定汇总：9 条中 7 条完全属实、1 条半修（状态直变）、1 条降级处置（Excel）。未发现任何「声称已修而未修」的虚报。**

---

## 一、总体结论

| 维度 | 评级 | 一句话 |
|---|---|---|
| 对局引擎（双规则） | ✅ 健康 | 真实 UI 合同穷举全绿；baseAp/DEFAULT_GOAL/起手加抽三处旧账已清 |
| 叙事运行时 / 存档 | ✅ 良好 | 结算竞态已修；存档版本+形状校验+隐私容错齐备 |
| 美术落位 | ✅ 卡图满配 / ⚠️ 场景仍稀 | 卡图/立绘/封面/结局图零缺口；场景底图 ≈15% |
| 经济系统 | ✅ 本轮无软锁面 | 钥匙卡禁卖生效；离市零门槛；赌坊输光不阻塞流程 |
| 剧情门控（本轮重点） | ⚠️ 一处运气门 | 秦淮「楼庸结局」被一次性行令的随机胜负门控（见 2.1） |
| 校验体系 | ✅ 对局/门控强 / ⚠️ 引用盲区 | 四组元断言落地；但市集/翻牌/卡包引用仍无校验（见 2.2） |
| 工程卫生 | ⚠️ 小账若干 | 状态直变遗留、图鉴分母不一致、行令消耗实现错误（未触发）等 |

---

## 二、本轮新发现（按优先级）

### 2.1 【P2】秦淮「参议楼庸」结局被一次性行令的运气门控

机制链条（逐行核实）：
- 「参议楼庸」是秦淮三结局之一、账证链最全的正解向分支，门槛 `statAtLeast: { shenwang: 55 }`（`qinhuai.ts:342-346`）。
- 全剧本 `shenwang` **唯一正增量**是行令胜利场景 `jl_win` 的 `+5`（`qinhuai.ts:299`），初始 50 → 恰好 55，无任何冗余余量。
- 行令是**一次性**的：入口挂 `notFlag: "done_jiuling"`，进即置旗（`qinhuai.ts:165`），败走 `jl_lose` 后不可重试。
- 行令胜负依赖随机令签：组合博弈核算（对手任意发令、玩家最优应对）判定该配置（手牌「策器势隐策」、3 轮、需 ≥3 分）**仅「可能取胜」而非「必然可胜」**——存在令签序列使玩家无论如何出牌都拿不满 3 分。

后果：玩家若在河畔酒肆运气不好，**本局永久失去最完整证据链结局**，且无任何提示告知该选择已关闭。这不是软锁（另两个指控选项不受影响），但把「结局质量」交给了不可控随机，与本作「选择塑造结局」的核心体验相悖。
修复方向（任选）：① 行令允许重试（旗标改为胜后置位）；② 给 `shenwang` 增加一处主线必得的 +5（如宴前对局胜利）；③ 门槛降至 50。建议 ①，同时保留「输了也能推进」的现状。

### 2.2 【P2】市集/翻牌/卡包/压制的引用完整性仍是校验盲区

`verify.mts` 检查了 `duel.deck`、`duel.script`（压制）、效果/条件引用的卡与线索，但**没有检查**：
- `shop.stock` 与 `packs.pool` 引用的卡是否存在；
- `cardPick.options` 引用的卡是否存在；
- 翻牌选项是否恰为三张（类型注释承诺「三选一」）。

后果分层：货架/卡包引用错卡 → `def(id)` 返回 `undefined` → UI **静默跳过**（玩家看不到，作者也收不到报错）；翻牌引用错卡 → 翻牌界面少一张；最坏情形是错卡 id 进入背包后，`BagView` 的 `const c = def(id)!`（`App.tsx:910`）在渲染时对 `undefined` 取属性**直接崩溃**。
本轮已用一次性脚本全量实测：**当前 13 部数据零错误**——所以现在不修也不会炸，但这正是「数据干净时最便宜」的断言。建议 `verify` 增三行遍历即可（货架∪卡包池∪翻牌选项 ⊆ 卡牌表；翻牌选项数=3）。

### 2.3 【P2】`jiulingPlay` 消耗手牌的实现错误（当前数据未触发，属潜伏雷）

`minigames.ts:141`：
```ts
st.hand = st.hand.filter((c) => c !== cardSuit || st.hand.filter((x) => x === cardSuit).length > 1)
  .slice(0, Math.max(0, st.hand.length - 1));
```
语义推演：当所出花色的持有数 ≥ 2 时，`filter` 保留全部手牌，随后 `slice(0, len-1)` 截掉的是**数组末位元素**——可能是另一花色。即「打出策牌却丢掉器牌」。现行唯一行令配置的手牌结构（策×2 且策恰在末位）恰好不触发，故玩家无感；但只要新增任何「重复花色不在数组末尾」的行令配置，玩家手牌就会被错误消耗，直接劣化胜率。修复是一行：找到第一张 `cardSuit` 的索引删除即可。

### 2.4 【P3】卡图图鉴的进度分母不一致，收集率永远到不了 100%

`CardGallery` 头部：`已收录 {seen.size}/{sc.cards.length} 张（资源卡不计入收集）`（`App.tsx:1391`）。分母 `sc.cards.length` **含资源卡**，而资源卡翻到即折银、永不入袋、永不被 `recordCardsSeen` 记录——分子永远追不上分母，且文案自我矛盾（括号说不计入，分母却计入）。对照：标题页入口与图鉴列表的分母都正确地排除了资源卡（`App.tsx:607,1594`）。另外图鉴分组里资源卡会以永久「？？？/尚未收录」呈现，同样是该矛盾的产物。修复：分母改用 `collectible.length`，资源卡组从展示中剔除或加「不计收集」标注。

### 2.5 【P3】气力类加成的文案与生效面不符 + 情绪制回气无上限

- 帝国加成「底气十足」（`b_qi`：本局**全部对局**气力上限 +2）与人物卡 `bonusQi`（携带：气力上限 +X）实际只作用于压制制 `hpPlayer`（`duel.ts:90`）；情绪制 `qi` 恒为初始 3（`duel.ts:87`），加成不落。文案承诺的「全部对局」与实现不符。
- 情绪制「回气」物品 `qi += 3` 无上限（`duel.ts:252`），可叠过 10，`QiBar` 按 max=10 渲染会显示「13/10」式的越界数字。
影响不大（加成对情绪制本就不关键），但属于「描述超卖」+「数值越界展示」两颗小地雷。建议：文案改为「压制制气力上限 +2」，或让 `initDuel` 对情绪制 `qi` 同样叠加；`qi` 加 10 上限钳制。

### 2.6 【P3】其余工程小账（单条不致命，合并记账）

1. **苏十娘货栈的 0 两孤品**：`i_gongxiang`（孤品）`price: 0` 且**在货架**（`qinhuai.ts:145`），`buy` 按 `c.price ?? 10` 结算 → 0 两可得。若是剧情赠予请保留，但 `price: 0` 与 SCHEMA「非卖品不填（即留空）」的约定冲突，且下一位按文档制表的作者会把「非卖品填 0」放上货架白送。建议：`buy` 对 `price === 0` 显式视为非卖，或数据改用留空 + 剧情效果赠卡。
2. **物品卡使用不推进回合 → 该动作不触发自动存档**：自动存档依赖 `[st, phase, duel?.round, duel?.finished]`（`App.tsx:430`），而 `playItem` 不 `round += 1`。情绪/压制局里刚用掉道具就关页，重进会回到用道具前（道具每局重置故不破坏平衡，但战况回退体验怪异）。依赖里补 `duel?.usedCards.length` 即可。
3. **卡包孤品可重复开出，静默吞银**：`pack_jia` 池含 4 张孤品（sichou 等），重复开出时 `openPack` 静默跳过不入袋、不退款、不提示。购前无「已拥有」警示。建议在开包结果上对重复卡加「已有」角标，或池内过滤已拥有。
4. **`RunState.cards` 是只写不读的遗留字段**：`initState` 赋值后仅被存档形状校验读取（`save.ts:49`），玩法零消费；经典模式的「全池即手牌」实际走 `cfg.deck`。建议删除或注释「仅为旧档兼容」。
5. **状态直变（上轮遗留）**：`DuelView`/`MiniGameView` 仍是引擎 in-place mutate + `{...duel}` 浅拷贝。当前能跑，引入 memo/并发渲染即爆。维持上轮建议：引擎返回新状态或视图先克隆。
6. **一次性脚本留存**：`_sim_emotion_v2.mts`、`_dump_art.mts`、`_audit_coverage.mts` 仍在 `scripts/`。前两个已无复用价值；`_audit_coverage.mts` 本轮仍在出力，建议转正并纳入 `verify` 后置步骤（资产缺口 > 阈值即警告）。
7. **`minigames.ts:6` 注释笔误**：骰宝注释写「大(4-10)/小(11-17)」，与代码判定（`sum>=11` 为大）及游戏内文案均相反。仅注释错误，顺手改。

---

## 三、值得保留的加分项（对抗审计也要记功）

1. **`verify.mts` 从「对局合同」升级为「门控合同」**：四组元断言（实得线索对账 / 钥匙卡获得途径 / 数值理论上限 / 小游戏合法性）全部落地并写进注释与文档，本轮我用独立脚本复核的结论与其一致——它已是本项目可信度最高的资产。
2. **美术接入一夜补齐 63 张叙事卡图**：上轮最大缺口（叙事 8 部零卡图）本轮实测归零，且全部 137 卡 + 26 立绘按 id 精确落位，`CardArt` 零代码改动即消费——资产投放流程已被验证可规模化，剩余场景图复用同法即可。
3. **钥匙卡禁卖的实现方式正确**：不是硬编码黑名单，而是运行时扫描 `cond.card/notCard` 动态建集——未来新增钥匙卡自动受保护，这比上轮建议的「数据断言」更稳。
4. **引擎数值卫生**：`DEFAULT_GOAL`/`DEFAULT_AP`/`baseAp` 三处常量化后，缺省值分裂类地雷已清零；四色克制环在引擎 / UI / 行令 / SCHEMA / 规则书五处表述仍一致，无版本漂移。
5. **存档竞态的修法干净**：不靠延长窗口或加锁，而是「结束即推进结算场景」，让存档永远落在稳定态——这是正确的结构性修法。

---

## 四、风险优先级清单

| 优先级 | 问题 | 影响 | 修复方向 |
|---|---|---|---|
| P2 | 秦淮楼庸结局被一次性行令运气门控 | 最完整证据链结局可能因随机令签永久关闭且无提示 | 行令可重试 / 增加主线必得的 shenwang +5 / 门槛降 50 |
| P2 | 市集/翻牌/卡包引用无校验 | 错 id 静默降级，最坏背包渲染崩溃 | `verify` 增三组引用断言（十行内） |
| P2 | `jiulingPlay` 消耗错牌（潜伏） | 新增行令配置即触发，劣化胜率 | 改为按索引删除第一张所出花色 |
| P3 | 图鉴分母含资源卡，进度永不满 | 收集系统「永远 99%」的错觉 | 分母改 `collectible.length` |
| P3 | 气力加成文案超卖 / 回气无上限 | 描述与实现不符、数值越界展示 | 文案改压制制限定或补情绪制生效；`qi` 钳制上限 |
| P3 | 0 两孤品上架 / 道具回合不存档 / 卡包重复静默 / `RunState.cards` 死字段 / 状态直变 / 注释笔误 | 卫生与维护税 | 见 2.6 逐条 |
| 遗留 | 场景图覆盖 ≈15%（缺 203） | 正文场景大多无底图 | 沿用上轮策略：每剧本挑开局/对决/结局 3-5 节点投放，槽位就绪即生效 |

---

## 五、行动建议（落地顺序）

1. **半天内（全是一行至十行改动）**：① `jiulingPlay` 改按索引删牌（2.3）；② `verify` 增市集/翻牌引用断言（2.2）；③ 图鉴分母修正（2.4）；④ `qi` 上限钳制 + 加成文案（2.5）；⑤ 自动存档依赖补 `usedCards.length`（2.6-2）。
2. **本周（数据调参）**：⑥ 处置秦淮运气门（2.1），推荐行令胜后置旗可重试；⑦ 决定 `i_gongxiang` 是赠予还是非卖（2.6-1），并在 SCHEMA 补一句「price=0 与留空的语义」。
3. **资产投放（并行）**：⑧ 场景图按「每剧本 3-5 关键节点」继续铺，优先案件 5 部的对局场景与叙事 8 部的结局前节点。
4. **回归**：改动后跑 `verify + tsc -b + vite build`，并对 qinhuai 做一次「行令故意输 → 回到酒肆入口确认可重试 → 赢 → 指控楼庸」的浏览器走查。

---

*本报告基于通读 `app/src/**`、`docs/SCHEMA.md`、`scripts/*` 及五组实测：`verify.mts`（0 失败/0 警告）、`tsc -b`（退出码 0）、`vite build`（成功）、`_audit_coverage.mts`（卡图/立绘 0 缺口）、三个一次性对抗脚本（引用完整性 0 命中 / cond 传播与数值门槛逐条核算 / 行令博弈可胜性判定，脚本已删除、方法与结论留存于本报告）。与上轮报告不一致处（场景图覆盖进展、钥匙卡处置方式）已在第〇章显式标注。*
