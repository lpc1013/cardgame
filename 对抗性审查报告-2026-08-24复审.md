# 帝成观止 · 对抗性审查报告（2026-08-24 复审）

> 审计对象：`E:\CardGame\app`（Vite + React 19 + TS 叙事卡牌引擎）+ `src/assets/**`（美术落位）+ `E:\CardGame\scripts_txt`、`E:\CardGame\art`
> 审计立场：**对抗性复审** —— 以上一份《卡牌系统对抗审计报告》（2026-08-23）的修复回执为基准，逐条复核并寻找新暴露面。
> 审计方式：通读 `src/engine/*`、`src/App.tsx`（1668 行）、`src/data/*` 抽样、`scripts/content.mjs`、`docs/SCHEMA.md` + **运行时实测**（`verify.mts`、`tsc -b`、`vite build`、资产覆盖率脚本 `scripts/_audit_coverage.mts`）。
> 结论一句话：**上轮 P0 全部修复属实、核心对局健康；本轮未发现新的不可玩级缺陷，但美术接入「只铺了案件剧本」、市集可卖钥匙卡、Excel 管线与 SCHEMA 承诺的剪刀差扩大，是三条新的主要风险线。**

---

## 〇、上轮回执复核（先验旧账）

| 上轮修复项 | 本轮复核 | 判定 |
|---|---|---|
| 情绪制 v2 真实 UI 软锁（P0） | `playEmotion` v2 分支已不扣 AP（`duel.ts:165-171`）；`doEndTurn` 早退已删（`App.tsx:1080-1084`）；换气按钮仅压制制显示（`App.tsx:1155-1157`） | ✅ 属实 |
| 校验套件虚假绿灯（P0） | `verify.mts` 情绪制穷举**不含**换气动作（第 83-123 行注释明写历史教训），`stateKey` 含 `hand/library/discard/usedCards`；实测 **0 失败 / 0 警告** | ✅ 属实 |
| 牌库洗回 / 起手补牌统一 | 打出进弃牌堆、抽空调洗回填（`drawUp`）、起手与每轮均补至 4 | ✅ 属实 |
| 人物卡占手牌位 | 人物卡不进牌库、开局场外生效（`initDuel:72-73`） | ✅ 属实 |
| 美术接入（74 卡图 + 25 立绘 + 11 场景） | 卡图/立绘/封面/结局图齐；**但场景图现为 35 张、仅覆盖约 14% 场景，且叙事剧本成术卡零卡图**（见 二·2.1） | ⚠️ 部分属实 |
| Excel 管线（暂缓，用户决策） | 缺口仍在，且**比上轮记录的更大**（见 二·2.3） | ⏸ 待办，需重估 |

**基础健康度实测**：`node --experimental-strip-types scripts/verify.mts` → 0 失败 / 0 警告（含 8 处设计性死局按预期标注）；`tsc -b` 退出码 0；`vite build` 成功（PWA 预缓存 11 项）。

---

## 一、总体结论

| 维度 | 评级 | 一句话 |
|---|---|---|
| 对局引擎（双规则） | ✅ 健康 | 真实 UI 合同穷举全绿；四色克制/洗回/被动自洽 |
| 叙事运行时 / 存档 | ✅ 良好 | 旗标/条件/效果通用可组合；存档有版本+形状校验+隐私容错 |
| 美术落位 | ⚠️ 半覆盖 | 案件 5 部满配；叙事 8 部卡图为零、场景图覆盖 14% |
| 经济系统（市集/银两） | ⚠️ 有软锁面 | **钥匙卡可被卖掉**，分支永久关闭（实测命中 1 例） |
| 内容管线（Excel） | ❌ 与承诺脱节 | SCHEMA.md 承诺的 v2 全列，`content.mjs` 未实现；往返即有损 |
| 校验体系 | ✅ 对局强 / ⚠️ 元盲区 | 对局可解性断言扎实；但 cond 可达性、复盘门槛、小游戏未覆盖 |
| 工程卫生 | ⚠️ 小账若干 | 状态直变（mutation）、死代码、默认值分裂、根 README 空 |

---

## 二、本轮新发现（按优先级）

### 2.1 【P1】美术接入只铺了「案件 5 部」，叙事 8 部仍是文字卡

实测（`scripts/_audit_coverage.mts`，按 `<id>.png` 精确核对）：

| 资产 | 落位数 | 缺口 |
|---|---|---|
| 卡图（`assets/cards`） | 74 | **63 张成术卡缺图**：全部集中在 jieyu / shumian / changjiang / diaolan / changhen / jianfeng / xingxing / touming（8 部叙事剧本），案件 5 部 0 缺口 |
| 立绘（`assets/portraits`） | 25 | 对手/角色覆盖正常 |
| 场景图（`assets/scenes`） | 35 | **207 个场景无底图**（覆盖率 ≈ 14%，且多为封面级节点，正文场景基本无图） |
| 封面 / 结局图 | 13 / 33 | 13 部封面全齐；结局图 0 缺口 ✅ |

后果：8 部叙事剧本的对局仍是纯文字卡，与「卡牌系统 v2 全卡有画」的玩家预期不一致；场景底图稀疏使 `scene-bg` 层大部分时间空置。**上轮回执「美术已接入」需修正为「案件线已接入，叙事线未铺」**。好消息是 `CardArt`/`sceneArt` 槽位全部就绪，补图即生效，无需代码改动。

### 2.2 【P1】市集可卖「钥匙卡」：软锁面实测命中 1 例

- 实测命中：`fuma.ts:246` 金陵夜市货架含 `i_yu`（龙凤玉珏），而 `fuma.ts:386` 存在 `cond: { card: "i_yu" }` 的关键选项（追问玉珏来历 → `a_yu_probe` 支线）。玩家**购得或翻牌获得后再卖掉**（`ShopView.sell`，`App.tsx:912-920`），该支线本局永久关闭。当前影响为「支线+道义值损失」（非通关必需），未构成硬软锁，但这是**机制级漏洞**而非数据级疏忽。
- 同类隐患：全库共 5 处 `cond: { card: ... }`（fuma×3、sichou×1、xie×1）。目前仅 `i_yu` 上了货架，但**没有任何校验阻止未来的钥匙卡上架**。
- 更深的对抗点：`sell` 只查 `sc.cards`，行囊携带的跨剧本物品在背包里无卖出入口——行为不一致（同屏一半卡可卖一半不可卖）。
- 修复方向（任选）：① 钥匙卡（被 `cond.card`/`cond.notCard` 引用者）禁卖并加角标；② `verify.mts` 增加「货架 ∩ 条件钥匙卡 = ∅」断言；③ 市集禁卖 `rarity:"孤品"` 与物品层剧情卡。建议 ②+① 并行。

### 2.3 【P1】Excel 管线与 SCHEMA.md 的剪刀差比上轮记录更大

上轮记录了 8 个卡牌字段丢失；本轮逐函数核对 `content.mjs`，缺口清单**扩大**：

| 层 | 丢失字段 |
|---|---|
| 卡牌表 | `layer / rarity / itemEffect / passive / resource / price / image / theme`（导出 `toRows:62-65`、导入 `doImport:158` 均只有 id/name/suit/text/lore/power）；**导入连 `cost` 都丢**（导出有、导入无，单向丢） |
| 场景表 | `cardPick / shop / minigame / next2` 四列完全缺失 —— **市集、翻牌、小游戏在 Excel 里无处可写** |
| 选项条件 | `fmtCond/parseCond` 不含 `card / notCard / resourceAtLeast` —— 「卡牌即钥匙」往返即蒸发 |
| 效果 | `fmtEffect` 不含 `removeCard / gainSilver / spendSilver`（导出静默返回 ""） |
| 对局表 | `rules / oppCards` 缺失（v2 剧本导出后退化为 classic） |
| 总表 | `cardSystem / deckLimit / initialDeck / initialSilver` 缺失 |

而 `docs/SCHEMA.md` 第 40-66 行**白纸黑字承诺了上述全部列**。即：文档描述的合同 > 管线实现的合同，任何按文档制表的新剧本作者都会得到一份被静默降级的数据。这比「缺功能」更危险——它是**错误承诺**。建议：要么本期补齐管线，要么在 SCHEMA.md 顶部加「⚠ 以下 v2 列尚未被 content.mjs 支持，仅 .ts 直编可用」的显式红字。

### 2.4 【P2】校验体系的元盲区（对局之外不设防）

`verify.mts` 的对局穷举是本次审计里质量最高的资产，但其防线**止于对局**：

1. **可达性不建模 cond**：图遍历把所有选项边视为无条件可达（`edgesOf:60-74`）。若某分支的唯一入口挂了一个永不可满足的 `cond`，verify 依然绿灯——条件型软锁完全在盲区。建议补「旗标/线索/卡牌的可获得性传播」轻模型，或至少断言「每个 cond.card 的卡在剧本内存在获得途径」。
2. **复盘门槛不对账实得线索**：只断言 `mustPick ≤ 线索总数`（`verify:191`），未断言「存在一条真实流程能解锁 ≥ mustPick 条线索」。若某案流程最多只能拿到 2 条线索而 mustPick=3，「呈上御案」按钮将**永久禁用**（`App.tsx:683` `disabled={picked.length !== v.mustPick}`），这是典型硬软锁形态。
3. **小游戏可解性零断言**：棋局残局 `answer` 索引越界、行令手牌不可胜（需 `score ≥ rounds`），均无校验。现存两处（fuma 残局 / qinhuai 行令）经人工核读可解，但规则上无保障。
4. **市集/翻牌引用与钥匙卡检查**：本轮由审计脚本代劳（命中 2.2），应内置进 `verify.mts`。

### 2.5 【P2】引擎与 UI 的小账（单条不致命，合并起来是维护税）

1. **`endTurn` 把行动力硬置回 3**（`duel.ts:202` `st.ap = 3`）：帝国加成「行动力+1」（`b_ap`）只在首回合生效，第一次换气后永久丢失。应改为记录 `baseAp` 后回补。
2. **情绪制 goal 默认值分裂**：引擎 `st.cfg.goal ?? 5`（`duel.ts:173`），UI 共鸣点渲染 `duel.cfg.goal ?? 3`（`App.tsx:1102`）。现存数据全部显式填了 goal 故未触发，但这是一颗等着新剧本踩的地雷。
3. **DuelView 直接 mutate 对局状态**（`App.tsx:1061/1072` 直接对 `duel` 对象调引擎函数，再 `{ ...duel }` 浅拷贝触发渲染）。当前能跑，但依赖「引擎恰好改的是顶层字段」的巧合；React 状态对象被就地改写是典型隐患源（尤其将来引入 memo/并发渲染）。建议引擎函数改为返回新状态，或视图层先克隆再调。
4. **对局结束 1.6s 窗口期的存档竞态**（`App.tsx:285-294`）：`finished` 置位后自动存档写的是「对局场景 + 无对局进度」，此时关闭页面，读档会**重打刚赢/输掉的对局**。低概率但真实存在。
5. **`extraDraw` 被动起手不加、之后加**（`initDuel:96` 传了 `n` 导致 `drawUp` 的 `extra` 不生效；`endTurn/afterTurn` 不传 `n` 又生效）：手牌数在 4 与 4+extra 间震荡。建议起手同样结算被动。
6. **骰宝出千控骰可产出非法骰面**（`minigames.ts:29-32`：`total-a-b` 可能为 7 或 ≤0）；`sicboPayout` 的 `win && caught` 是死分支（caught 必为 lose）。展示层会打出「共 N 点」的怪骰。
7. **死代码与卫生**：`save.ts` 的 `warehouseAdd/warehouseRemove` 无调用方（行囊已改自动收藏，仓库概念仅存于旧档兼容读取）；根目录 `README.md` 仅两行占位；`scripts/_sim_emotion_v2.mts`、`_dump_art.mts` 为一次性脚本。建议删仓库 API 或在注释中声明「仅为旧档兼容保留」。
8. **数值条零消费**：13 部剧本的 `stats`（朝望/道义/民心等）**没有任何一处** `statAtLeast` 条件引用（grep 全库 0 命中），数值全程只进战报不影响分支。引擎能力闲置，玩家看到的数值变化无后果——要么接入结局评级，要么明说它是氛围值。

---

## 三、值得保留的加分项（对抗审计也要记功）

1. **`verify.mts` 的「真实 UI 合同」范式**：把上轮的教训固化成注释与断言（情绪制无换气、状态键含三堆），并用 `setDuelShuffle` 注入确定性——这是本项目目前最强的工程资产，建议继续扩权（见 2.4）。
2. **引擎纯净度**：`duel/runtime/minigames/save` 全部无 DOM 依赖、可被 node 直接驱动；`runtime.ts` 仅 109 行承载旗标/条件/效果全语义。
3. **存档防御**：版本号 + 形状校验 + 隐私模式全静默容错；旧档 `bag/deck/silver` 有补全兜底（`App.tsx:444-451`）。
4. **美术接入层的零侵入设计**：图缺失时纯文字降级，`_artUrl` 按 id 精确匹配——2.1 的补图工作因此是纯资产投放，无代码风险。
5. **四色体系一致性**：克制环在 `duel.ts / App.tsx / minigames.ts / SCHEMA.md / 规则书文案` 五处表述一致，未发现版本漂移。

---

## 四、风险优先级清单

| 优先级 | 问题 | 影响 | 修复方向 |
|---|---|---|---|
| P1 | 叙事 8 部卡图=0、场景图覆盖 14% | 「全卡有画」名不副实 | 按 `art_prompts.json` 补 63 张成术卡图；场景图按「每剧本 3-5 张关键节点」策略铺 |
| P1 | 市集可卖钥匙卡（fuma/i_yu 实测命中） | 支线永久关闭的软锁面 | 钥匙卡禁卖 + `verify` 加「货架∩钥匙卡=∅」断言 |
| P1 | Excel 管线缺口扩大且与 SCHEMA.md 承诺冲突 | 表格作者产出被静默降级 | 补齐管线；或先在 SCHEMA.md 标注未支持列 |
| P2 | verify 元盲区（cond 可达 / mustPick 实得对账 / 小游戏 / 市集引用） | 条件型软锁无防线 | 在现有套件上增四组断言 |
| P2 | `endTurn` 吞 `b_ap` 加成 | 付费/肝出的帝国加成部分失效 | `baseAp` 记录回补 |
| P2 | goal 默认值分裂（引擎 5 / UI 3） | 新剧本未填 goal 时 UI 与判定不一致 | 统一缺省（建议都取 5 并类型上必填） |
| P2 | DuelView 状态直变 + 1.6s 存档竞态 | 隐患源，未来重构易爆 | 引擎返回新状态；结束即存档一次最终态 |
| P3 | extraDraw 起手不一致 / 骰宝怪骰 / 死代码 / 数值条零消费 | 体验与卫生 | 顺手修 |
| P3 | 根 README 空置 | 新入口零信息 | 补一页速览 |

---

## 五、行动建议（落地顺序）

1. **半天内**：① 钥匙卡禁卖 + verify 断言（2.2）；② `endTurn` 回补 baseAp（2.5-1）；③ goal 缺省统一（2.5-2）。三处都是十行以内改动，直接消除两个软锁面与一颗地雷。
2. **本周**：④ `verify.mts` 增补四组元断言（2.4），把「条件型软锁」纳入自动防线；⑤ 决策 Excel 管线：补齐或降级承诺（2.3），不要让文档继续超卖。
3. **下周起（资产投放）**：⑥ 叙事 8 部卡图按「孤品/精 → 良 → 凡」顺序铺；场景图每剧本挑开局/对决/结局 3-5 个节点。`CardArt` 槽位就绪，投放即生效。
4. **回归**：全部修完后跑 `verify + tsc -b + vite build`，并对 fuma 做一次「买入 i_yu → 卖出 → 回到追问节点」的浏览器实测，确认禁卖生效。

---

*本报告基于通读 `app/src/**`、`docs/SCHEMA.md`、`scripts/*` 及三项实测：`verify.mts`（0 失败/0 警告）、`tsc -b` + `vite build`（通过）、`scripts/_audit_coverage.mts`（资产覆盖与钥匙卡核查，脚本已留存可复跑）。与上轮报告不一致处（美术接入范围、Excel 缺口规模）已在第〇、二章显式更正。*
