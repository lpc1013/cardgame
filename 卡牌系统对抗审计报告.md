# 帝成观止 · 卡牌系统对抗审计报告

> 审计对象：`E:\CardGame\app`（Vite + React + TS 叙事卡牌引擎）
> 审计范围：卡牌玩法 / 卡牌设计 / 卡牌风格 / 美术资源构建 / 玩法更新 / 相关系统搭建
> 审计方式：通读源码（`engine/`、`App.tsx`、`data/*`、`scripts/*`、`docs/SCHEMA.md`、`art/`、`scripts_txt/`）+ 运行时实测（node `--experimental-strip-types` 复现对局）
> 日期：2026-08-23

---

## 一、总体结论（先给结果）

**架构分层清晰、数据驱动思路正确，但「卡牌系统 v2」当前处于半残状态，且美术资源构建与产品本身严重错位。**

| 维度 | 评级 | 一句话 |
|---|---|---|
| 引擎分层 / 数据驱动 | ✅ 良好 | types→runtime→duel→save→sfx 职责清晰，剧情全表格化可维护 |
| 卡牌玩法（v2 对局） | ❌ 严重缺陷 | **情绪匹配制 v2 全部 6 局实测软锁**，无法通关 |
| 卡牌设计（四层模型） | ⚠️ 可用但有洞 | 模型优雅但平衡粗糙、Excel 管线丢字段 |
| 卡牌风格 | ⚠️ 极简到无 | 纯 CSS 文字卡，零卡面美术，稀有度仅一条边框色 |
| 美术资源构建 | ❌ 方向错位 | `art/` 是「古代写实战争电影」分镜，与权谋卡牌游戏无关；游戏本体无卡图 |
| 卡牌玩法更新机制 | ⚠️ 脆弱 | `r2.mjs` 用字符串替换直接改 `App.tsx`，不可维护 |
| 相关系统搭建 | ✅ 良好 | 存档/音效/小游戏/剧情树/校验齐全，但校验有盲区 |

**最关键两条（必须修）：**
1. **情绪制 v2 对局行动力（AP）只扣不回，全部软锁** —— 已实测 5 个案件 6 局全中。
2. **美术资源在做「另一个产品」** —— `art/战争分镜与角色生图提示词.txt` 与 `scripts_txt/古代写实战争风格分镜脚本及角色设定.txt` 是西楚霸王/陉谷大战的影视分镜，与「大理寺·驸马·秋闱」的权谋叙事卡牌毫无关联；而游戏本体 13 个剧本、~100 张卡，**没有任何一张卡有图像**。

---

## 二、卡牌玩法（对抗重点）

### 2.1 实测发现：情绪匹配制 v2 必定软锁【严重】

`src/engine/duel.ts` 的 `playEmotion` 在 `rules==="v2"` 分支里扣 AP（`st.ap -= cardCost(card)`），但**全代码没有任何路径在情绪制下回补 AP**：
- `endTurn()` 第一行 `if (st.rules !== "v2" || st.finished) return;` —— 情绪制根本不进；
- `afterTurn()` 只调 `drawUp`，不碰 AP；
- 对局视图里的「换气」按钮（`DuelView`）在情绪制下点击只 `toast("情绪对局按回合自动推进")` 然后 return，**是空操作**。

AP 初始恒为 3，每张成术牌费用 1–3。结果：**打 1–2 张就没 AP，既赢不了（共鸣不够）也输不了（气力不因不出牌下降）→ 死锁。**

**运行时实测（满编卡组、最优同色策略）：**

| 对局 | 模式 | goal | 终局共鸣 | 终局AP | 软锁 |
|---|---|---|---|---|---|
| fuma/d_wang | emotion v2 | 3 | 1 | 1 | ✅ |
| qiuwei/d_xiaomianhu | emotion v2 | 3 | 1 | 1 | ✅ |
| sichou/d_zhou | emotion v2 | 3 | 1 | 1 | ✅ |
| xie/d_zhifu | emotion v2 | 3 | 2 | 0 | ✅ |
| qinhuai/d_zhoumang | emotion v2 | 3 | 1 | 0 | ✅ |
| qinhuai/d_shishang | emotion v2 | 3 | 1 | 1 | ✅ |

**结论：5 个案件全部接入的 v2 情绪对局，玩家 100% 卡死。** 而情绪制（谈判/试探）恰是权谋题材最贴合、也是 5 案默认最多的对局类型——这意味着 v2 系统的核心体验目前是不可玩的。

> 注：`pressure`（气力压制制）v2 因 `endTurn` 正常回 AP，**未受影响**，可正常游玩。

### 2.2 校验套件没抓到这个 bug【系统盲区】

`scripts/verify.mts` 对情绪制只做了「花色覆盖 + 跑 400 步」，且循环里 `if (!ok) { endTurn(d); continue; }`——情绪制 `endTurn` 是空操作，于是它在 AP 耗尽后原地空转 400 次、**从不断言是否 finished**。CI 绿，bug 隐身。`build` 脚本 `node ... verify.mts && tsc -b && vite build` 因此放行了不可玩版本。

### 2.3 其他玩法风险

- **牌库不洗回（deck exhaustion）**：`drawUp` 只从 `library` 抽，无弃牌堆回收。17–22 张的小卡组在持久 pressure 局里约 6 回合抽光后手牌只减不增，长局可能无牌可打。
- **起手 4 张 vs 每回合补 3 张不一致**：`initDuel` 抽 4，之后 `afterTurn`/`endTurn` 只补到 3，第一回合隐性多 1 张。
- **人物卡占手牌位（pressure v2）**：人物卡不可打出，却会进牌库、占手牌槽，稀释可打出牌比例。
- **「抽牌」道具与 AP 的交互未定义**：`itemEffect:"抽牌"` 不消耗回合也不回 AP，在 AP 归零的情绪局里仍无效（见 2.1）。

---

## 三、卡牌设计（四层模型）

### 3.1 模型本身优雅【加分】

`CardLayer = 成术/物品/人物/资源` + `Rarity = 凡/良/孤品` + `suit/ power/ cost/ itemEffect/ passive/ resource` 是一套自洽、可扩展、支持「卡牌即钥匙」（`Cond.card`/`Effect.unlockCard/removeCard`）的设计。fuma 的 21 张卡叙事密度高（text+lore 双文本），是亮点。

### 3.2 设计层面的漏洞

- **费用自动折算与显式 cost 混用，平衡不可控**：`cardCost()` 在 `cost` 缺省时按 `power` 推（≥4→3，≥3→2，否则 1）。成术牌 power 2–4 对应费 1–3，但物品卡固定费 2。结果是「威牌 power4 费3 且压制制 ×2」与「普通牌费2」之间缺乏统一曲线，强度靠手调，无数据校验。
- **情绪制其实不需要费用**：既然情绪制已被「共鸣/破防/失言」三态定义清楚，叠加 AP 费用是过度设计且（见 2.1）实现崩了。建议情绪制 v2 **回归无限出牌**（与 classic 同逻辑，仅多了手牌/道具/被动的呈现），把 AP 机制专留给 pressure。
- **孤品（资源本位之外）缺乏辨识度**：`rarity-孤品` 仅改上边框色 + 背景微调，标题页/图鉴无「唯一」视觉强调，与「全剧唯一」的设定不匹配。
- **资源卡被排除在收藏与卡组外**：图鉴 `collectible` 过滤掉资源卡，但「孤品现世计数」文案暗示要数孤品——资源卡里的「官银三十两(rarity 良)」也出现在卡包，逻辑边界略乱。

---

## 四、卡牌风格（视觉）

### 4.1 现状：纯文字卡，无卡面美术

`app.css` 中卡牌全部是 `.play-card / .pick-card / .bag-card` 文字盒子：
- 花色用 4 色字（`威`红/`理`蓝/`利`金/`情`紫）；
- 稀有度 = 顶部 2px 边框色（凡灰/良金/孤品红+深色底）；
- 无卡框、无立绘、无卡背、无箔光、无动画（仅 hover 边框）。

UID 是「暗底羊皮 + Noto Serif SC 衬线」的极简文字风，**卡牌本身没有视觉身份**——在一款自称「卡牌系统 v2」的游戏里，这是核心短板。

### 4.2 与美术资源的方向冲突【关键】

见第五章。美术产线产出的是「8K 写实战争电影」素材，与「扁平衬线文字 UI」在调性上本就互斥——即便把那些分镜图塞进卡面，也会破坏现有 UI 一致性。

---

## 五、美术资源的构建（着重项）

### 5.1 事实核查

| 位置 | 内容 | 与游戏关系 |
|---|---|---|
| `E:\CardGame\art\战争分镜与角色生图提示词.txt` | 古代写实战争分镜 + AI 生图/图生视频提示词；主角/陉谷敌将/**西楚霸王**；陉谷伏击、楚歌四起、霸王突围 | **无关**——是战争史诗影视分镜 |
| `E:\CardGame\scripts_txt\古代写实战争风格分镜脚本及角色设定.txt` | 同上主题的另一份战争分镜 | **无关** |
| `scripts_txt/` 其余 14 篇 | 驸马/秋闱/丝绸/谢秀才/秦淮河堤/劫与烬 等 | ✅ 与游戏剧本对齐（叙事源稿） |
| `app/src/assets/` | 仅 `hero.png`(343×361) + `vite.svg` | 游戏**唯一**图像资产 |
| 全部 13 剧本 ~100 张卡 | 卡面 = `{name, text, lore}` 纯文本 | **零卡图** |

### 5.2 判定：美术资源在服务于「另一个产品」

1. **主题错位**：游戏是「中式权谋·大理寺查案·多结局叙事」，美术产线在做「楚汉战争·写实电影」。两者受众、调性、视觉语言完全不同。当前 `art/` 产出的任何图都不能直接进游戏。
2. **卡牌美术完全空白**：卡牌系统 v2 跑了一轮「玩法更新」，却**没有任何配套卡面/图标/稀有度视觉资产**的构建记录。美术资源构建与卡牌玩法更新是脱节的两条线。
3. **风格互斥**：提示词反复强调「8K 超高清电影级、写实背景、血污、甲胄」——这类写实厚涂若用于卡面，会与现有极简衬线文字 UI 严重冲突，需重做整套 UI 才能承接。

> 建议立即决策：**要么**美术线改做「权谋卡牌」资产（角色立绘 / 卡框 / 花色纹样 / 孤品箔光 / 市集与对局场景插画），**要么**承认当前游戏定位为「零图文字卡牌」，把 `art/` 的战争分镜移交/归档到独立项目，避免资源与认知混乱。

### 5.3 卡牌视觉资产应补的清单（若走卡牌美术线）

- 四花色纹样（威/理/利/情）与对应色板变量
- 三层稀有度卡框（凡/良/孤品）+ 孤品唯一标识
- 人物卡立绘位、物品卡图标位、资源卡银两纹样
- 对局/市集/翻牌三场景的氛围插画（非写实厚涂，建议水墨/工笔/版刻风以匹配衬线 UI）
- 卡背、抽牌/翻牌动效资源

---

## 六、卡牌玩法的更新机制（着重项）

### 6.1 `r2.mjs` 是直接改源码的「补丁脚本」【反模式】

`r2.mjs` 用 `readFileSync("src/App.tsx")` + `a.replace(...)` 字符串替换，往 `App.tsx` 注入「玩法速览」引导与战报字段。问题：
- 一旦 `App.tsx` 对应片段变动，replace 静默失败（无报错、无校验），引导层悄悄消失；
- 引导文案、战报 DOM 应属产品功能，不该由外部脚本事后打补丁；
- 不具备幂等与版本标记，二次运行可能重复插入或错位。

**建议**：把「玩法速览/战报」作为正式组件常驻 `App.tsx`，删除 `r2.mjs` 这类源改脚本。

### 6.2 v2 接入本身质量不均

- 5 个案件 `cardSystem:true` + `rules:"v2"` 接得齐，但如前所述**情绪制实现崩了**；
- `initialDeck` 仅 5 张（多为费 2 牌），在「AP 只扣不回」的前提下连 1 个情绪局都过不去——即便修了 AP，起手卡组强度也需重新标定。

---

## 七、相关系统的搭建（着重项）

### 7.1 扎实的部分【加分】

- **引擎分层**：`types / runtime / duel / save / sfx / minigames` 边界清楚，`applyEffects/checkCond` 通用、可组合。
- **存档体系**（`save.ts`）：版本号 + 形状校验 + 对局进度持久化 + 图鉴/剧情树/卡牌收集三套 localStore，迁移与容错（隐私模式静默）到位。
- **程序化音效**（`sfx.ts`）：WebAudio 零素材，古琴/木鱼/宫调意象贴合题材，可开关。
- **小游戏引擎**（`minigames.ts`）：骰宝（含出千）、棋局残局、宴会行令，纯状态机、UI 解耦，扩展性好。
- **剧情树**（`TreeView.tsx`）：SVG 分支可视化，已见/未见解锁，多结局引导清晰。
- **PWA**：`vite-plugin-pwa` 离线可装。

### 7.2 有洞的部分【需修】

- **`verify.mts` 盲区**：情绪制不断言可通关（见 2.2）；另 pressure 穷举里 `seen` 状态键未含 `library/discard`，理论上可能漏判，但当前小卡组影响有限。
- **Excel 内容管线丢字段**（`scripts/content.mjs`）：`toRows` 与 `doImport` 的 cards 只处理 `id/name/suit/text/lore/power/cost`，**完全丢失 `layer / rarity / itemEffect / passive / resource / price`**，也不处理 `cardPick / shop / minigame / oppCards / rules / deckLimit / initialDeck / initialSilver / cardSystem`。后果：README 承诺的「写新剧本不改代码、v2 全量卡牌表格维护」**对四层卡、市集、翻牌、小游戏不成立**——用 Excel 导出的 v2 卡会退化成只有成术牌。这是与「相关系统搭建」直接矛盾的能力缺口。
- **`loadAll()` 用 `Function(...)` 动态 eval** 解析 `.ts`：能用但脆弱、无类型安全，建议改走 `import()` 或构建期生成。

---

## 八、风险优先级清单

| 优先级 | 问题 | 影响 | 修复方向 |
|---|---|---|---|
| P0 | 情绪制 v2 全软锁 | 5 案核心对局 100% 不可玩 | 情绪制 v2 去掉 AP（回归无限出牌）；或 revealEmotion 时回 AP+补牌 |
| P0 | 美术资源与产品错位 | 美术投入无产出、方向混乱 | 决策：做权谋卡牌美术 / 或归档战争分镜到独立项目 |
| P1 | verify 不断言情绪可通关 | CI 绿但游戏坏 | verify 增加情绪制「最优策略必达 goal」断言 |
| P1 | Excel 管线丢 v2 字段 | 「不改代码写剧本」承诺失效 | content.mjs 补齐 layer/rarity/itemEffect/passive/resource/price 及市集/翻牌/小游戏列 |
| P2 | `r2.mjs` 源改反模式 | 引导层易静默丢失 | 常驻组件化，删脚本 |
| P2 | 牌库不洗回 / 起手 4 补 3 不一致 | 长局无牌、首回合隐性优势 | 加弃牌回收；统一抽牌数 |
| P2 | 孤品无唯一视觉 | 收藏动机弱 | 加箔光/编号/现世计数视觉 |
| P3 | 人物卡占 pressure 手牌位 | 稀释可打牌 | 人物卡不进抽牌堆，仅记被动 |

---

## 九、行动建议（落地路线）

1. **先救玩法（1 天内）**：情绪制 v2 改为「无限出牌 + 手牌/道具/被动呈现」，与 classic 逻辑对齐；同步在 `verify.mts` 加情绪制可通关断言并跑通。这能立刻让 5 案 v2 体验恢复。
2. **定美术方向（本周决策）**：与产品确认——是「零图文字卡牌」还是「权谋卡牌美术」。若为后者，依据第五章 5.3 清单产出水墨/工笔风卡面与场景插画，与现有衬线 UI 调性统一；若为前者，将 `art/` 战争分镜移出本项目归档。
3. **补系统缺口（1–2 周）**：content.mjs 补齐 v2 字段；`r2.mjs` 功能常驻化；牌库回收；孤品视觉强化。
4. **回归**：修完后跑 `npm run verify` + 5 案情绪/压制对局端到端实测，确认「标题 ✦ → 市集 → 翻牌 → 对局 → 复盘 → 结局 → 图鉴」全链路无软锁。

---

*本报告基于通读 `app/src/**`、`docs/SCHEMA.md`、`scripts/*`、`art/`、`scripts_txt/` 及运行时复现得出。所有「软锁」「丢字段」结论均有代码行号与实测输出支撑。*
