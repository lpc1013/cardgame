# WorkBuddy internal project data (not source)
.workbuddy/

# OS / editor
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Logs
*.log
npm-debug.log*

# Large generated art bundles (kept locally, not in git)
art/*.zip

# Optional: PNG art assets can be large; keep tracked but exclude if needed
# app/src/assets/
