# 实施计划 · 2026-08-26（全部 7 项决策已拍板）

> 状态：计划已定稿，等待用户点头后逐批执行。每批完成 → 三关验证 → 浏览器实测 → 汇报过目。
> 原则：所有改动必须用户点头后才实施；每个决策点已确认，本计划不再含未定项。

---

## 决策总览（7 项，全部 ✅）

| # | 决策 | 落点 |
|---|---|---|
| 1 | 犒军删池：删 `warPool`；刺探/收买绑定随从（斥候/内应），只扣次数不扣银两；无随从置灰 | 批次 A |
| 2 | 进度条六档：25%→40 / 40%→70 / 50%→100 / 66%→130 / 75%→160 / 100%→200 = 700 | 批次 A |
| 3 | 弱卡生态：只做「番外解锁硬需求」，弱卡=番外钥匙，挂卡册详情页；已有成就全保留 | 批次 C |
| 4 | 于谦奖励卡：j_sheji/j_shoujiu 保留原样；「社稷为重→海瑞」记档不执行 | 批次 E |
| 5 | 陷阱卡：先出池子三层审计报告（资产/数据/玩法），融入方案审计后定 | 批次 D |
| 6 | 成就 UI：删 deck tab 下第二排 13 剧本行（顶部 5 tab 保留）；弹窗统一 ×1.5 | 批次 B |
| 7 | 出征界面：左池右槽 + 左池卡面网格（160px 品级边框），右槽 5 格插槽可视化 | 批次 B |

---

## 批次 D · 池子审计（先行，只读不改数据）

**目标**：全量检查策/势/器/隐卡片是否在它们应该在的池子里，输出"抽到却永远用不上"清单，为陷阱融入方案提供数据依据。

**任务 D1｜资产层：目录 ↔ 卡表归属**
- 复用 `scripts/card_audit.mts`（卡表×资产一致性）
- 检查项：成术按 `suit` 进 `cards/策|器|势|隐`；物品/人物/资源/孤品进 `cards/gu`；根目录零散放；无跨目录重复；卡表 id ↔ 资产文件一一对应

**任务 D2｜数据层：layer/suit 语义一致性 + 悬空引用**
- `trap` 字段只能出现在**隐色成术**上（揪出器色陷阱之类错位）
- 卡包 `pool` / 商店 `stock` / 对局 `deck` / 随身位可携带物，引用的卡 id 必须存在于卡表（防悬空）
- 资源卡 `suit` 合理性（资源卡是否混入成术语义）

**任务 D3｜玩法层：可用池覆盖（新增维度）**
- 每张卡至少落在一个"可用池"：对局 deck / 随身位可携带（物品+人物）/ 商店 stock / 卡包 pool
- 输出「持有/抽到却永远无法上场」清单（已知：10 张案件陷阱卡只进卡包 → 重灾区）

**产出**：审计报告（分层清单 + 统计 + 问题卡 id 列表）→ 呈现给用户 → 用户据此拍板陷阱融入方案（随身位陷阱格 / 案件 deck 编入 / 对手盖陷阱）。

**验证**：脚本 0 崩溃；报告逐层可读；不动任何数据。

---

## 批次 A · 机制（犒军删池 + 随从动作重构 + 进度条六档）

### A1｜删犒军银池
- `src/data/jieyu.ts:55`：删 `warPool: 30`
- `src/engine/duel.ts`：删 `silverPool` 字段、`initDuel` 中 `cfg.warPool ?? 0` 初始化、`duelSpend` 的 pool 分支与"犒军银池见底了"文案
- `src/engine/types.ts`：删 `warPool` 相关注释/字段定义

### A2｜刺探/收买重构：绑定随从、只扣次数
- `src/engine/duel.ts` `duelSpend(st, kind)`：签名去掉 `silver/source`；**强制要求** `retinueNames.length > 0`（无随从直接返回 `{ok:false, log:"需携带斥候/内应随从"}`）；只扣 `scoutLeft/insiderLeft/sharedUsed`，不扣银两
- 次数额度保留：凡=斥候1 · 良=内应1 · 精=双能共1 · 传=双能共2
- **行为变更**：v2（剧本银两）局以前可用银两直接刺探/收买 → 现在统一必须有随从；全模式（classic/v2）无随从即禁用

### A3｜前端同步
- `src/App.tsx` `doSpend`：删 `source` 判断与银两扣减；刺探/收买按钮在 `duel.retinueNames.length === 0` 时置灰 + 提示"需带斥候/内应随从"
- `src/App.tsx:1641`：删"犒军银池 X 两"显示
- 规则书文案（App.tsx guide-sec）："战争局另有犒军银池直购"→"刺探/收买需携带斥候/内应随从，消耗次数，不耗银两"

### A4｜进度条六档
- `src/engine/save.ts` `END_MILES` 改为：
  `[{pct:0.25,ink:40},{pct:0.4,ink:70},{pct:0.5,ink:100},{pct:0.66,ink:130},{pct:0.75,ink:160},{pct:1,ink:200}]`
- 档位↔结局数对照（TOTAL_ENDS=76，Math.ceil）：25%→19 / 40%→31 / 50%→38 / 66%→51 / 75%→57 / 100%→76
- `claimableMile` 遍历逻辑不变（自动适配新档位）；旧存档 `endMile` 语义兼容（已领最高档索引）

**验证**：verify 0/0 → tsc -b → `mv dist dist_old && vite build` → 3000 端口实测：无随从进对局按钮置灰；带随从只扣次数银两不动；进度条新档位（31/51 等阈值）显示与领取正常。

---

## 批次 B · UI（弹窗 ×1.5 + 成就删剧本行 + 出征左池右槽）

### B1｜弹窗统一 ×1.5（`src/app.css`）
| 面板 | 现值 | 改后 |
|---|---|---|
| `.bag-panel`（行囊/商市/通用） | 900px | 1350px |
| `.gallery-panel`（图鉴/卡册/成就） | 860px | 1290px |
| `.prep-panel`（出征） | 560px | 840px |
| `.settings-panel`（设置） | 480px | 720px |
- 规则书面板随 `.bag-panel` 一并加宽；检查 `max-height: 85vh/88vh` 与滚动不受影响

### B2｜成就面板删第二排剧本行（`src/App.tsx` AchievementsPanel）
- 删：`scF` state、`ach-tabs-sub` JSX 块（13 剧本按钮）、`scDone()` 函数
- 保留：顶部 5 tab（卡组/对局/小游戏/收集/隐藏）、弱卡点名仍按 `category="deck"` 全部显示
- `AchievementDef.scenario` 字段保留（详情弹层仍显示归属剧本，仅删筛选 UI）

### B3｜出征界面：左池右槽 + 卡面网格（`src/App.tsx` PrepModal + `app.css`）
- 面板 840px 宽，布局改为左右分栏：
  - **左池（候选区）**：物品/随从/奖励卡三分区，卡面网格渲染（160px 缩略图 + 品级边框色 + 随从次数徽标）；点选=高亮+角标 ✓
  - **右槽（随身位）**：5 格插槽可视化——空槽虚线框，放入后显示卡面缩略图；物/人分格样式区分（随身位物/人争位规则直接可见）
  - 奖励卡区（deckBonus）保持独立折叠；和议之书保持原逻辑
- 卡面缩略图复用现有 `card-art-compact` 资产路径，品级色沿用固定色板（凡#565040·良#5b8fb8·精#8b7ab8·传#d2a44f·孤品#e0745a）

**验证**：三关 + 浏览器实测：成就面板无第二排；各弹窗宽度；出征携带/取消、超格拦截、右槽插槽显示。

---

## 批次 C · 内容（番外系统：弱卡解锁）

> 前置：实施前先调研现有剧情结构（`Scenario.scenes`/`viewpoints`/`endings` 形态），确定番外数据承载方式。

- **数据**：番外定义（id / 标题 / 钥匙卡 id / 解锁条件=携带该卡赢下对局 / 番外剧情场景）
- **判定钩子**：对局胜利结算处（扩展 `checkDuelAchievements` 或新增 `checkBonusUnlocks`）——`deck.includes(钥匙卡) && 胜利` → 解锁番外（写存档，幂等）
- **UI**：卡册详情页（卡 zoom 弹层）新增"番外"入口：已解锁→可进番外剧情；未解锁→显示"携带此卡赢下对局解锁"
- **内容**：首批番外剧情文本先出框架与 1-2 篇样例，按"批批准后一次性执行"逐批审

**验证**：三关 + 实测解锁→进入→阅读全流程。

---

## 批次 E · 长期记档（不实施，仅记录）

- **未用卡定期重做**：设想——使用率审计脚本 + 周期重做机制；游戏尚处早期，"长期"未发生，暂不实施，记入长期计划
- **社稷为重 → 海瑞线**：已记档（本日日志），做海瑞线时再议
- **钥匙卡白色**：已实施（凡级化），确认为长期设计（最不起眼的物件发挥最大作用）

---

## 全局工程约束（每批必守）

1. **落库流程**：改 `src/data|engine` → `verify.mts`（0 失败 0 警告）→ `tsc -b` → `vite build`
2. **build 坑**：safe-delete trash shim 拦 dist 清空 → `mv dist dist_old_xxx && vite build`（dist 不存在即无清空），build 后删 dist_old_*；**必须看完整日志确认 `dist/assets/index-*.js` 生成**
3. **浏览器实测**：固定 `http://127.0.0.1:3000/`（静态 dist 服务），不重开新端口；测前确认 dist 最新
4. **文件锁**：Windows 下 mv/cp 报 Permission denied 就重试或换目录名
5. **评审**：番外等文本内容送审通过才落库

## 执行顺序

D（审计报告）→ 用户拍板陷阱融入 → A（机制）→ B（UI）→ C（番外）→ E（记档）。每批完成汇报，用户点头后进入下一批。
