# 帝成观止 · 卡牌系统对抗审计报告（更新版）

> 审计对象：`E:\CardGame\app`（Vite + React + TS 叙事卡牌引擎）+ `E:\CardGame\art`（美术产线）+ `E:\CardGame\scripts_txt`（剧本源稿）
> 审计范围：**卡牌玩法 / 卡牌设计 / 卡牌风格 / 美术资源构建 / 玩法更新机制 / 相关系统搭建**（着重项）
> 审计方式：通读 `src/engine/*`、`src/App.tsx`、`src/data/*`、`docs/SCHEMA.md`、`scripts/*`、`art/*` + **运行时实测复现**（node `--experimental-strip-types` 忠实模拟对局）
> 日期：2026-08-23（v2：基于今晨美术产线更新后的再核验，修正初版两处诊断）
> 配套实测：`scripts/_sim_emotion_v2.mts`（本报告结论的可复现证据）

---

## 〇、与初版报告的关键修正（先说清楚，避免误导）

| 初版结论 | 本次复核 | 修正 |
|---|---|---|
| 软锁根因 = `endTurn` 函数「情绪制根本不进」 | `endTurn` 仅以 `rules==="v2"` 门控，对情绪制 v2 **正常回 AP** | 真正根因在 **UI 层 `App.tsx:856` `doEndTurn` 对 emotion 早退**，从不调用 `endTurn` |
| 美术「方向错位」列为 P0 未解 | 战争分镜已归档 `_archive_war/`，新增 `权谋卡牌美术提示词.md` + `art_prompts.json` + 12 张风格样张 | 方向已转舵为「权谋卡牌」，但**仍未接入游戏**（卡面美术=0）。P0 降级为「方向已定·接入未做」 |
| 情绪制 v2「6/6 软锁」 | 实测 6 个中 **5 个硬软锁 + 1 个（xie/d_zhifu）仅侥幸可胜** | 系统仍崩，但需更精确表述 |
| 校验「盲区」 | 实测 `verify.mts` 退出码 **0（CI 绿）** | 本质更严重：测试在模拟里调用了 `endTurn`，**校验的是一份真实 UI 不存在的合同** |

---

## 一、总体结论（先给结果）

**引擎分层清晰、数据驱动思路正确、美术方向已转舵；但情绪制 v2 对局在真实 UI 下几乎全软锁，且美术资产与游戏本体仍「两张皮」。**

| 维度 | 评级 | 一句话 |
|---|---|---|
| 引擎分层 / 数据驱动 | ✅ 良好 | types→runtime→duel→save→sfx 职责清晰，剧情全表格化 |
| 卡牌玩法（v2 对局） | ❌ 严重缺陷 | **5/6 情绪制 v2 真实 UI 下软锁**，根因在 UI 换气按钮禁用于情绪制 |
| 卡牌设计（四层模型） | ⚠️ 可用但有洞 | 模型优雅；Excel 管线丢 8 个 v2 关键字段，四层卡退化为纯成术 |
| 卡牌风格（游戏内） | ⚠️ 极简到无 | 纯 CSS 文字卡，零卡面图；稀有度仅一条 2px 顶边框 |
| 美术资源构建（产线） | ⚠️ 方向对·接入缺 | 权谋卡牌提示词体系完整 + 12 样张，但**未落到 `src/assets/`**，游戏内卡图=0 |
| 卡牌玩法更新机制 | ❌ 反模式 | `r2.mjs` 字符串替换直改 `App.tsx`；引导层已注入但脆弱 |
| 相关系统搭建 | ✅ 良好 | 存档/音效/小游戏/剧情树齐全；`content.mjs` 字段丢失是能力缺口 |

**最关键两条（必须修）：**
1. **情绪制 v2 真实 UI 软锁（5/6）** — 玩家 100% 卡死在 5 个案件的核心对局类型。
2. **美术资产「生成了但不接入」** — `art/` 有完整提示词与样张，游戏 `src/assets/cards` 空置，卡牌仍是纯文字。

---

## 二、卡牌玩法（对抗重点 · 已实测）

### 2.1 实测：情绪制 v2 在真实 UI 下 5/6 软锁【严重，P0】

复现脚本 `scripts/_sim_emotion_v2.mts` 对 `mode==="emotion" && rules==="v2"` 的 6 个对局分别按「真实 UI（换气按钮被禁）」与「理想 UI（可换气）」建模：

| 对局 | 真实 UI（换气禁） | 终局共鸣/goal | 终局AP | 理想 UI（可换气） |
|---|---|---|---|---|
| fuma/d_wang | **死锁** | 1/3 | 0 | win (3步) |
| qiuwei/d_xiaomianhu | **死锁** | 1/3 | 1 | win (3步) |
| sichou/d_zhou | **死锁** | 1/3 | 1 | win (3步) |
| xie/d_zhifu | 侥幸 win | 3/3 | 0 | win (3步) |
| qinhuai/d_zhoumang | **死锁** | 1/3 | 0 | win (3步) |
| qinhuai/d_shishang | **死锁** | 1/3 | 1 | win (3步) |

**机制链条（根因精确定位）：**
- `playEmotion` 在 v2 分支扣 AP（`st.ap -= cardCost(card)`），且 `afterTurn` 只 `drawUp` 不回 AP → AP 单向递减。
- 玩家要回 AP 必须点「换气」→ `App.tsx:855-860`：
  ```ts
  const doEndTurn = () => {
    if (duel.mode === "emotion") { toast("情绪对局按回合自动推进"); return; } // ← 早退，永不 endTurn
    endTurn(duel);
  };
  ```
- 于是：初始 AP=3，打 1–2 张成术牌（费 2 居多）即耗尽；既赢不了（共鸣不够）也输不了（不出牌气力不降）→ **死锁**。
- 注意：`endTurn` 函数本身对 `rules==="v2"` 完全开放（重置 AP+补牌），所以修复点**不在** duel 引擎，而在 UI 这行早退。初版报告把根因误归到 `endTurn`，特此更正。

### 2.2 校验套件给出虚假绿灯【元级缺陷，P0】

`scripts/verify.mts` 退出码 **0（CI 通过）**，但其情绪制模拟（第 130–140 行）在 `playEmotion` 返回 `false`（AP 不足）时**直接调用 `endTurn(d)` 回 AP**：
```ts
const ok = playEmotion(d, c);
if (!ok) { endTurn(d); continue; }   // ← 建模了一个真实 UI 不存在的「可换气情绪局」
```
即**测试验证的是一份真实代码并未履行的 UI 合同**。比「缺测试」更糟：它给了「已通过」的假象，让不可玩版本随 `build` 脚本（`verify && tsc -b && vite build`）放行上线。这是对抗审计最该标记的结构性风险——**测试与实现对「情绪制能否换气」存在事实分歧**。

### 2.3 其他玩法风险

- **牌库不洗回（deck exhaustion）**：`drawUp` 只会从 `library` 抽、无弃牌堆回收。17–22 张小卡组在持久 pressure 局约 6 回合抽光后手牌只减不增。
- **起手 4 张 vs 每回合补 3 张不一致**：`initDuel` 抽 4，之后 `afterTurn/endTurn` 补到 3，首回合隐性多 1 张。
- **人物卡占手牌位（pressure v2）**：人物卡不可打出却进牌库、占手牌槽，稀释可打牌比例。
- **「抽牌」道具与 AP 交互未定义**：`itemEffect:"抽牌"` 不耗回合也不回 AP，在 AP 归零的情绪局里仍无效（见 2.1）。

---

## 三、卡牌设计（四层模型）

### 3.1 模型本身优雅【加分】

`CardLayer = 成术/物品/人物/资源` + `Rarity = 凡/良/孤品` + `suit/power/cost/itemEffect/passive/resource/price` 自洽、可扩展，支持「卡牌即钥匙」（`Cond.card` / `Effect.unlockCard/removeCard`）。fuma 的 21 张卡 `text+lore` 双文本叙事密度高。

### 3.2 设计层漏洞

- **费用自动折算与显式 cost 混用**：`cardCost()` 在 `cost` 缺省时按 `power` 推（≥4→3 / ≥3→2 / 否则 1），物品卡固定费 2。成术牌 power2–4 对应费 1–3，与「威牌 power4 费3 且压制制×2」之间无统一曲线，强度靠手调、无数据校验。
- **情绪制其实不需要费用**：情绪制已被「共鸣/破防/失言」三态定义清楚，叠加 AP 费用是过度设计且（见 2.1）实现崩了。建议情绪制 v2 **回归无限出牌**（与 classic 同逻辑，仅多出手牌/道具/被动呈现），把 AP 专留给 pressure。
- **孤品缺乏唯一视觉**：`rarity-孤品` 仅改顶边框色 + 深色底（app.css:208），图鉴无「唯一」强调，与「全剧唯一」设定不匹配。
- **资源卡收藏边界略乱**：图鉴 `collectible` 过滤掉资源卡，但「孤品现世计数」文案暗示要数孤品；资源卡里的「官银三十两(良)」也出现在卡包，边界不清。

### 3.3 Excel 内容管线丢 v2 字段【能力缺口，P1】

`scripts/content.mjs` 的 `toRows`（第 62–65 行）与 `doImport`（第 158 行）对 cards 只处理：
```
id / name / suit / text / lore / power / cost
```
**完全丢失** `layer / rarity / itemEffect / passive / resource / price / image / theme`。后果：README 承诺的「写新剧本不改代码、v2 全量卡牌表格维护」**对四层卡、市集、翻牌、小游戏不成立**——用 Excel 导出的 v2 卡会退化成只剩成术牌（物品/人物/资源/稀有度全没）。这与「相关系统搭建」直接矛盾。

---

## 四、卡牌风格（视觉 · 游戏内）

### 4.1 现状：纯文字卡，零卡面图

`app/styles`（`app.css`）中卡牌全是 `.play-card` 文字盒子；`CardArt` 组件（`App.tsx:64`）在找不到图时返回 `null`，`.card-art` 仅留一条暗色占位（app.css:323-328）。`src/assets/cards`、`portraits`、`scenes` 三个目录**均为空 `.gitkeep`**。即：全部 ~100 张卡 + 角色 + 场景，**游戏内视觉身份为零**。

UID 是「暗底羊皮 + Noto Serif SC 衬线」的极简文字风，卡牌本身没有卡面美术——在自称「卡牌系统 v2」的游戏里是核心短板。

### 4.2 已设计但未落地的解法

`gen_art_prompts.mts` 提出两套解耦方案，方向正确：
- **甲·去字化**：卡面主体=独立插画，策/器/势 三类不印汉字，仅作「色相 + 非字纹章」由 UI 叠加 → 根治跨剧本「满屏同一词」的频率感。
- **乙·双轴门类**：卡面主类目改为 13 类「门类」词汇（刑名/盐铁/漕运/科举/闺阁…），由 `cardThemes.ts` 映射，单一词出现率降一个数量级。

但这两套方案**只存在于提示词文档与 `cardThemes.ts` 查表**，游戏渲染层尚未消费——`App.tsx` 里 `theme-tag`、`suit-seal` 已叠加文字，卡图却空着。

---

## 五、美术资源的构建（着重项 · 今晨已转舵）

### 5.1 事实核查（当前）

| 位置 | 内容 | 与游戏关系 |
|---|---|---|
| `art/_archive_war/战争分镜与角色生图提示词.txt` | 西楚霸王/陉谷大战影视分镜 | **已归档**，移出主产线 |
| `art/_archive_war/app_art_README_战争分镜指引.md` | 战争分镜旧指引 | 归档说明 |
| `art/权谋卡牌美术提示词.md`（115KB） | 权谋卡牌完整提示词（甲+乙、104 张覆盖、角色、11 场景、引擎接入说明） | 产线输出，**未接入** |
| `art/art_prompts.json`（114KB） | 上述结构化版（cards/characters/scenes） | 产线输出，**未接入** |
| `art/samples/*.png`（12 张） | 多风格样张：工笔公堂/二次元文官/水墨大写意/宋明青绿/哑光电影明史剧/新中式卡牌插画… | 风格探索样张，**未命名落位** |
| `app/src/assets/{cards,portraits,scenes}` | 空 `.gitkeep` | 游戏实际取图目录，**零图** |
| `app/scripts/gen_art_prompts.mts` | 由剧本数据生成提示词的脚本（数据驱动，可复跑） | 良好工程实践 |
| `app/scripts/_dump_art.mts` | 仅打印卡牌清单的调试脚本，非接入 | 无接入作用 |

### 5.2 判定

1. **方向已正确转舵**：从「楚汉战争写实电影」转为「权谋卡牌·二次元古风厚涂 + 水墨融合 anime」，且以「去字化 + 门类双轴」解耦文字与画面，规避了初版担忧的「写实厚涂与衬线 UI 互斥」。**美术产线这一项应给正面评价**，与初版 P0「方向错位」已不同。
2. **接入是断点**：提示词再完整，只要图没落到 `src/assets/cards/<cardId>.png`、`portraits/`、`scenes/`，游戏内卡面就仍是纯文字。当前 `CardArt` 的 `import.meta.glob("./assets/cards/*.png")` 命中 0 文件。
3. **样张含风格噪音**：`art/samples/` 里仍有「哑光电影感明代历史剧」「明代大理寺公堂_8K写实厚涂」等写实风样张，与最终选定的 anime/水墨方向不一致，易混淆后续生图批次。
4. **无批量接入脚本**：`gen_art_prompts.mts` 只产出提示词，从「提示词→出图→按 `<cardId>.png` 落位→提交 assets」的链路没有自动化，依赖手工。

### 5.3 若走卡牌美术线（建议），需落地的清单

- 按 `art_prompts.json.cards` 把每张卡出图，命名为 `<cardId>.png` 落入 `src/assets/cards/`（人物/对手落 `portraits/`、场景落 `scenes/`）。
- 建议先铺「孤品 + 良」约 30 张跑通视觉与接入，再补 凡。
- 清理 `art/samples/` 中写实风样张，或对样张加方向标注，避免误导。
- 接入口碑：现有 `CardArt` + `.card-art` 槽位已就绪，补图即生效；`suit-seal`/`theme-tag` 已叠加文字，无需改卡面文字逻辑。

---

## 六、卡牌玩法的更新机制（着重项）

### 6.1 `r2.mjs` 是直接改源码的「补丁脚本」【反模式，P2】

`r2.mjs` 用 `readFileSync("src/App.tsx")` + `a.replace(...)` 字符串替换，注入「玩法速览」引导与战报字段。当前 `App.tsx` 的 `showGuide` 状态与 `guide-sec` 确为其注入产物（已生效）。问题：
- 片段一旦变动，`replace` 静默失败（无报错、无校验），引导层悄悄消失；
- 不具备幂等，二次运行可能重复插入或错位；
- 引导/战报属产品功能，不该由外部脚本事后打补丁。

**建议**：把「玩法速览/战报」作为正式组件常驻 `App.tsx`，删除 `r2.mjs`。

### 6.2 v2 接入质量不均

- 5 个案件 `cardSystem:true` + `rules:"v2"` 接得齐，但情绪制实现崩（见 2.1）；
- `initialDeck` 仅 5 张（多费 2 牌）：即便修了 AP 回补，起手卡组强度也需重新标定（裸卡组审计显示部分案件花色覆盖不全，如谢案「势」缺失）。

---

## 七、相关系统的搭建（着重项）

### 7.1 扎实的部分【加分】

- **引擎分层**：`types/runtime/duel/save/sfx/minigames` 边界清楚，`applyEffects/checkCond` 通用可组合。
- **存档体系**（`save.ts`）：版本号 + 形状校验 + 三套 localStore（剧情树/卡牌收集/对局进度）+ 隐私模式容错。
- **程序化音效**（`sfx.ts`）：WebAudio 零素材，古琴/木鱼意象贴合题材，可开关。
- **小游戏引擎**（`minigames.ts`）：骰宝（含出千）/棋局残局/宴会行令，纯状态机、UI 解耦。
- **剧情树**（`TreeView.tsx`）：SVG 分支可视化，多结局引导清晰。
- **PWA**：`vite-plugin-pwa` 离线可装。
- **美术产线脚本**（`gen_art_prompts.mts`）：数据驱动生成提示词，可随卡表复跑，工程实践良好。

### 7.2 有洞的部分【需修】

- **`verify.mts` 盲区 + 测试/实现分歧**：情绪制不断言真实 UI 可通关（见 2.2）；pressure 穷举 `seen` 键未含 `library/discard`，理论上可能漏判（当前小卡组影响有限）。
- **Excel 字段丢失**（见 3.3）：`content.mjs` 让「不改代码写剧本」承诺对 v2 失效。
- **`loadAll()` 用 `Function(...)` 动态 eval** 解析 `.ts`：能用但脆弱、无类型安全，建议构建期生成或改 `import()`。

---

## 八、风险优先级清单

| 优先级 | 问题 | 影响 | 修复方向 |
|---|---|---|---|
| P0 | 情绪制 v2 真实 UI 软锁（5/6） | 5 案核心对局 100% 不可玩 | `App.tsx:856` 情绪制也调用 `endTurn`（或情绪制 v2 改为无限出牌）；可在 `doEndTurn` 去早退 |
| P0 | 校验套件虚假绿灯（测试≠实现） | CI 绿但游戏坏，误导发布 | `verify.mts` 情绪制模拟**不得**私自 `endTurn`；改为断言「真实 UI 合同」下可通关（即需先修 UI 才能过） |
| P1 | 美术资产生成了但不接入 | 卡面美术=0，投入无产出 | 按 `art_prompts.json` 出图落 `src/assets/{cards,portraits,scenes}`，先铺孤品+良 |
| P1 | Excel 管线丢 v2 字段 | 「不改代码写剧本」承诺失效 | `content.mjs` 补 `layer/rarity/itemEffect/passive/resource/price` 及市集/翻牌/小游戏列 |
| P2 | `r2.mjs` 源改反模式 | 引导层易静默丢失 | 常驻组件化，删脚本 |
| P2 | 牌库不洗回 / 起手 4 补 3 不一致 | 长局无牌、首回合隐性优势 | 加弃牌回收；统一抽牌数 |
| P2 | 孤品无唯一视觉 | 收藏动机弱 | 加箔光/编号/现世计数视觉 |
| P3 | 人物卡占 pressure 手牌位 | 稀释可打牌 | 人物卡不进抽牌堆，仅记被动 |
| P3 | `art/samples/` 含写实风噪音 | 误导后续生图批次 | 清理或加方向标注 |

---

## 九、行动建议（落地路线）

1. **先救玩法（1 天内）**：`doEndTurn` 去早退，情绪制 v2 也走 `endTurn` 回 AP；同步把 `verify.mts` 改成「**不**在模拟里偷偷 endTurn、改断言真实 UI 合同下可通关」，跑通 6 局全绿。这一步让 5 案 v2 体验立即恢复，且 CI 不再说谎。
2. **定美术接入（本周）**：依据 `art_prompts.json` 出图并落入 `src/assets/`（先孤品+良 ~30 张）。`CardArt` 槽位已就绪，补图即生效。清理 `samples/` 写实噪音。若决定暂不做卡面，则把 `art/` 战争分镜与未接提示词明确标注为「资料库/暂未接入」，避免认知混乱。
3. **补系统缺口（1–2 周）**：`content.mjs` 补齐 v2 字段；`r2.mjs` 功能常驻化；牌库回收；孤品视觉强化；`loadAll` 改 `import()`。
4. **回归**：修完后跑 `npm run verify` + 5 案情绪/压制对局端到端实测，确认「标题 ✦ → 市集 → 翻牌 → 对局 → 复盘 → 结局 → 图鉴」全链路无软锁。

---

---

## 十、修复回执（2026-08-23 当日处置，四色重设计同步完成）

| 优先级 | 问题 | 处置 | 说明 |
|---|---|---|---|
| P0 | 情绪制 v2 真实 UI 软锁 | ✅ 已修 | 采用报告建议的备选方案：情绪制 v2 **出牌不耗行动力**（AP 专留压制制）；`doEndTurn` 早退删除；换气按钮仅压制制显示 |
| P0 | 校验套件虚假绿灯 | ✅ 已修 | `verify.mts` 重写为**真实 UI 合同**断言：情绪制模拟无换气通道；压制制含出牌+换气；stateKey 含 hand/library/discard；新增裸卡组四色覆盖检查（报告 7.2 的盲区一并修复） |
| P1 | 美术资产不接入 | ✅ 已接入 | 74 卡图 + 25 立绘 + 11 场景图已落 `src/assets/{cards,portraits,scenes}` 并被 `CardArt`/`scene-bg` 消费 |
| P1 | Excel 管线丢 v2 字段 | ⏸ 暂缓（用户决策） | 往返实测确认缺口仍在（导入丢 `layer/rarity/itemEffect/passive/resource/price` 及 `cardSystem/initialDeck/shop/cardPick/rules`）；本轮明确不改 `content.mjs`，保留为待办 |
| P2 | `r2.mjs` 源改反模式 | ✅ 已修 | 引导层/战报常驻 `App.tsx`，`r2.mjs` 已删除 |
| P2 | 牌库不洗回 / 起手 4 补 3 不一致 | ✅ 已修 | 打出进弃牌堆，牌库抽空调洗回填；起手/每轮统一补至 4 张 |
| P2 | 孤品无唯一视觉 | ✅ 已修 | 孤品暗红底 + 箔光扫过动画；良卡金边内光；全卡位花色左缘色带（四色） |
| P3 | 人物卡占手牌位 | ✅ 已修 | 人物卡不进牌库，开局即场外生效（被动照常计入） |
| P3 | `art/samples/` 写实噪音 | — 保留 | 作为风格探索档案保留，不影响产线 |
| — | 附加：四色体系重设计 | ✅ 完成 | 新增「隐」色，单向克制环 策→势→器→隐→策；13 部剧本全量重标；压制制克制修正±1；行令小游戏同步四色；存档升 v3；verify 0 失败/0 警告（含 8 处设计性死局保持不可胜）；浏览器端到端实测通过 |

*回执基于：`npm run verify` 0 失败/0 警告、`tsc -b` + `vite build` 通过、Browser 端到端实测（驸马案全流程无报错无卡死）。*

---

*本报告基于通读 `app/src/**`、`docs/SCHEMA.md`、`scripts/*`、`art/**`、`scripts_txt/**` 及 `scripts/_sim_emotion_v2.mts` 运行时复现得出。所有「软锁」「丢字段」「未接入」结论均有代码行号与实测输出支撑；与初版不符处已在第〇章显式更正。*

